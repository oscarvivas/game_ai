# Copy this file to .env and fill in values.
# NEVER commit .env — it is in .gitignore.

# Optional: AI model API key (only if using external AI service)
# OPENAI_API_KEY=sk-...

# Optional: Analytics or feature-flag service
# ANALYTICS_KEY=
